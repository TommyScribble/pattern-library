import React from 'react';
import PageLayout from '../components/PageLayout';

const buttons = props => {
  return (
    <PageLayout>
      <div>This is the buttons page</div>
    </PageLayout>
  );
};

export default buttons;
